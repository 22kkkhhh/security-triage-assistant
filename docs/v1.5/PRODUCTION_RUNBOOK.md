# v1.5 Production Deployment Runbook（已弃用）

> **请改用最新文档：** [`docs/v1.12/PRODUCTION_RUNBOOK.md`](../v1.12/PRODUCTION_RUNBOOK.md)
>
> v1.12 起正式生产启动路径为 `npm start`（含 migration gate、`/api/health`、`/api/ready`）。
> 本文件保留仅作历史指向，避免两份 active runbook 冲突。
