# Security Triage Assistant UI 设计规范

版本：v2.1
状态：已批准，作为后续 UI 开发的基线
更新日期：2026-08-28
适用范围：`src/app/**`、`src/components/**` 以及与页面交互直接相关的样式和前端测试

## 1. 设计上下文

### 1.1 产品定位

Security Triage Assistant 是“数据与网络安全联合研判及案件运营助手”。它连接已有安全平台与人工研判流程，负责导入、标准化、补充上下文、辅助研判、证据整理、人工确认、案件运营和报告编辑；它不是 SIEM、SOC、IDS、NDR、DLP、漏洞扫描器或自动阻断平台。

### 1.2 用户与使用场景

- 主要用户：安全分析师、值班人员、案件负责人和系统管理员。
- 主要场景：长时间桌面端处理告警、查看风险、补充业务合理性、整理证据、提交人工研判和跟踪截止时间。
- 使用特征：信息密度高、需要反复阅读和比对，但最终决定必须由人作出。

### 1.3 品牌与审美方向

关键词：克制、清晰、任务优先、高密度但不拥挤、专业、安全、长期使用不疲劳。

采用“企业级 Security / Observability 工作台”语言：深海军蓝导航 + 中性浅色工作区 + 低饱和状态色。禁止赛博朋克、霓虹发光、渐变文字、装饰性大图和大面积玻璃效果。

## 2. 不可违背的原则

1. **一屏一个主任务**：首屏先回答“发生了什么、风险是什么、下一步做什么”。
2. **决策优先、渐进披露**：关键结论和行动在前；上下文、证据、审计和原始数据按需展开。
3. **证据优先于结论**：系统建议不能替代人工结论，技术异常不能直接等同于安全事件。
4. **语义色而非装饰色**：颜色只表达状态、风险和交互；颜色不能成为唯一状态信息。
5. **缺失数据显式表示**：`UNKNOWN` 表示“当前没有足够数据判断”，不等于正常或低风险。
6. **控制认知负荷**：默认 Comfortable density；不为了填满空间而添加 KPI、图表、卡片或快捷入口。
7. **长期可维护**：页面只组合统一 tokens 和组件，不在页面内散落颜色、字号、圆角和 z-index。

禁止在同一首屏同时出现完整 Dashboard、完整案件列表和完整案件详情。

## 3. 设计 Tokens

以下是规范值。实现时应集中定义为 CSS/Tailwind 语义 token，不得在页面组件中重复硬编码。

### 3.1 颜色

| Token | 值 | 用途 |
| --- | --- | --- |
| `--color-bg` | `#F6F8FA` | 工作区背景 |
| `--color-surface` | `#FFFFFF` | 面板、输入框、浮层 |
| `--color-surface-secondary` | `#F8FAFC` | 次级区域、表头、空状态底 |
| `--color-border` | `#E4E8ED` | 默认边框、分隔线 |
| `--color-text-primary` | `#17212B` | 标题、主要数据 |
| `--color-text-secondary` | `#687383` | 辅助信息 |
| `--color-text-muted` | `#8B95A3` | 占位、说明、禁用 |
| `--color-brand` | `#2F6FED` | 主操作、链接、选中 |
| `--color-brand-hover` | `#275FCC` | 悬停 |
| `--color-brand-selected` | `#EEF4FF` | 选中背景 |
| `--color-critical` | 低饱和暗红 | 严重/高风险 |
| `--color-warning` | 低饱和暖橙 | 中风险、待处理 |
| `--color-success` | 柔和绿 | 已完成、正常 |
| `--color-info` | 冷蓝 | 信息提示 |
| `--color-unknown` | 中性灰 | 暂无法评级 |

中性色占页面视觉约 85%–90%，品牌蓝约 5%–10%，状态色小于 5%。`UNKNOWN` 不得使用绿色；`OVERDUE` 是时间状态，不等于 HIGH RISK。风险色只用于小 Badge、2–3px 指示条、图标和少量数字，不得铺满整张卡片或整行列表。

### 3.2 字体与行高

中文优先使用：`system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", "PingFang SC", "Microsoft YaHei", sans-serif`。

| 层级 | 字号/字重 | 行高 | 用途 |
| --- | --- | --- | --- |
| Page Title | 24px / 600 | 28px | 页面主标题 |
| Case Title / H1 | 20px / 600 | 28px | 案件标题 |
| Section Title / H2 | 16px / 600 | 24px | 区块标题 |
| Body | 14px / 400 | 22px | 正文、主要数据 |
| Secondary | 13px / 400 | 20px | IP、账号、更新时间 |
| Caption / Badge | 12px / 400–500 | 18px | 标签、表头、提示 |

按钮使用 14px/500；Case ID、IP、Rule ID、Hash、端口等技术字段可使用 `ui-monospace`，但不得让整个系统变成等宽字体。

### 3.3 间距、尺寸与层级

- 4px 基准间距：`4, 8, 12, 16, 20, 24, 32, 40, 48`。
- 图标与文字 8px；同组字段 12px；控件间 8–12px；面板内边距 20–24px；区块间 28–32px；页面左右内边距 24–32px。
- Desktop 内容最大宽度 1200–1360px；案件详情正文 960–1120px。
- 圆角：小控件 4px，按钮/输入框 6px，Panel/Card 8px，Modal 10–12px。禁止大面积 `rounded-2xl`/`rounded-3xl`。
- 默认无阴影；Panel 使用 1px 边框。只有 Dropdown、Popover、Modal、Drawer 等浮层使用轻微阴影。

### 3.4 Icon、控件与浮层

- 统一使用 Lucide（或经批准的同一套图标库），线宽 1.5–2px。
- 图标尺寸：16px（表格/按钮）、20px（导航/区块）、24px（空状态/重点提示）；32px 以上仅限特殊空状态。
- 按钮只保留 Primary、Secondary、Ghost。默认高度 36px，Compact 32px，重要表单 40px；每页原则上最多 1 个 Primary。
- 输入框和 Select 高度 36px，Search 高度 40px；Focus 使用 2px 低强度蓝色 ring，不使用 Glow。
- Badge 高 22–24px，水平 padding 6–8px，圆角 4px，字号 12/500；同一区域最多 2–3 个 Badge。
- z-index：Base 0、Sticky 10、Dropdown 30、Drawer 40、Modal 50、Toast 60、Tooltip 70；禁止随意使用 `z-index: 99999`。

## 4. 信息架构与页面预算

### 4.1 四级信息层级

1. **Decision**：案件、当前状态、当前风险、下一步行动。
2. **Context**：账号、IP、系统、来源、负责人、截止时间。
3. **Evidence**：证据、Checklist、关联案件、历史信号。
4. **Audit / Detail**：Timeline、Audit Log、原始数据、技术细节，默认折叠。

### 4.2 Dashboard / 总览

总览只回答：“今天我需要关注什么？”首屏预算固定为：最多 3 个 KPI、1 个趋势、1 个 Need Attention 列表。快捷操作最多 1 个 Primary + 2 个 Secondary；其余操作进入“更多”或对应业务页。不得加入 8 个 KPI、多个装饰性图表或完整案件表。

日期范围与“今日到期/已逾期”等指标必须明确时间语义；同一屏不得让用户猜测统计口径。

### 4.3 案件列表

使用行式队列，不做 Excel 式十列宽表。单行 76–88px，优先显示：标题、Case ID、风险、状态、账号/IP/来源、负责人、Deadline、Pending；其他信息进入详情。移动端转为堆叠行。

### 4.4 案件详情

固定顺序：Case Header → 下一步行动 → Evidence Summary → Investigation Summary → 更多调查信息。默认单栏主 Workspace；桌面端最多 80/20 的 Context rail，且可收起。适合独立 Panel 的只有“下一步行动”和“调查摘要”；负责人、IP、来源、状态、时间使用 metadata group，不要每个字段都做卡片。

### 4.5 用户管理

用户管理的唯一主任务是“安全地维护可登录账号”。首屏按“页面说明 → 创建用户 → 已创建账号”组织，不使用早期后台常见的整页平铺表单。

- 创建表单按身份信息（用户名、显示名称、邮箱、角色）与凭证信息（初始密码、确认密码）分组；用户名和邮箱必须明确标注“创建后不可修改”。
- 用户列表优先显示用户、邮箱、角色、状态、创建时间和操作；显示名称允许行内编辑，角色使用 Select，状态使用 Badge + 具体动作。
- 重置密码属于敏感操作，默认折叠，点击“重置密码”后再展示输入框；停用账号、吊销会话和重置密码均不得提供物理删除入口。
- 创建、保存、启停、重置均需有明确的 loading / success / error 反馈；至少保留一个启用的管理员，受保护动作要解释原因。
- 首屏只保留一个 Primary 操作“创建用户”；其余操作使用 Secondary / Ghost 层级，避免每行出现多个同权重按钮。
- 用户身份使用“显示名称 + @用户名”的身份块；默认展示文本，点击“编辑名称”后才进入输入状态，保存/取消应就近出现。
- 行级启停、吊销会话和重置密码统一收进“更多操作”菜单；菜单外只展示状态和一个操作入口，避免表格变成控件墙。
- 敏感操作展开后必须提供明确的“取消”或“收起”路径；取消时清空未提交的凭证输入，并同时关闭相关菜单层级。

### 4.6 一级导航

一级菜单固定为：总览（若已提供独立总览页）、案件、告警接入、报告、管理。Checklist、Compliance、History、Audit、Related Cases 属于案件内部，不单独占一级导航。告警接入下的批量接入与原始告警属于同一接入域，使用可展开的二级入口；不把“新建研判”或原始数据查询提升为一级主导航。

账户设置属于当前登录用户的个人设置，不占一级业务导航；应放在应用右上角的用户菜单中，与当前用户、角色信息和退出登录相邻。移动端保留同一用户菜单入口。业务页标题统一复用 `PageHeader` 的字号、说明宽度和间距，避免同类页面各自定义一套标题风格。

管理类入口使用独立分组与业务入口分隔；用户管理属于“管理”分组，不与总览、案件队列、报告中心并列竞争首要注意力。

“管理”分组标题与业务导航保持同字号、同高度、同基础文字色，并提供可见的展开/收起状态；其子项仅做轻量缩进，不使用更小或更淡的标签样式。

业务页默认直接从主标题开始；只有确实需要表达上位分类时才使用眉题（eyebrow），不得为了装饰在标题前增加额外层级或空白。

桌面端顶部工作台栏固定为“居中快速访问 → 右上角用户菜单”的顺序，不额外保留左侧页面名，避免与页面主标题重复。快速访问必须连接到现有页面或真实筛选能力，并支持 `Ctrl/⌘ K` 与 `Escape`；没有数据源时不放虚假的通知数量、帮助入口或不可用搜索框。账户设置与退出登录只在右上角用户菜单出现，移动端使用同一菜单逻辑。

登录页复用工作台的深墨蓝、钴蓝和冷白色语义，但保持独立的登录场景：使用低对比、中心留白的抽象安全纹理作为背景，登录卡片必须维持清晰对比和可读性。背景素材不得包含真实系统、客户标识、可读文字或高亮装饰。

## 5. 组件与状态契约

基础组件清单：`Button`、`IconButton`、`Badge`、`Input`、`Select`、`SearchInput`、`Tabs`、`PageHeader`、`SectionHeader`、`CaseRow`、`Metadata`、`EmptyState`、`Skeleton`、`Alert`、`Drawer`、`Modal`、`Tooltip`、`Dropdown`、`Pagination`、`Checkbox`、`FormField`、`Stat`。

每个交互组件必须定义 Default、Hover、Active、Selected、Focus、Disabled、Loading、Error 八种状态。按钮加载文案使用“保存中…”等明确反馈；页面/区块加载优先 Skeleton；错误文案使用“暂时无法加载案件”+“重试”，技术异常只进日志。普通保存优先 inline “已保存”，重要动作才 Toast。Modal 仅用于危险操作、明确确认或集中输入，普通编辑优先 inline；Drawer 用于快速查看证据/关联案件/审计详情，不承载主调查流程。

## 6. 图标、背景与素材规则

- 图标优先来自统一图标库，不用截图、emoji 或混杂风格的第三方图标。
- 工作区背景只使用中性纯色或极轻的纹理；禁止渐变、发光网格、粒子和大面积装饰背景。
- 空状态可以使用小型线性图标或项目内 SVG；它必须解释“为什么为空”和可选下一步，不只是装饰。
- 位图素材若经批准，放在 `public/` 的明确子目录并保留来源、尺寸和替代文本；不得把真实客户截图、真实企业标识或生产数据写入仓库。
- 所有图标按钮必须有 `aria-label`；有意义的图片必须有 alt，纯装饰图片使用空 alt。

## 7. 响应式与无障碍

- Mobile `<768px`、Tablet `768–1199px`、Desktop `>=1200px`、Wide `>=1600px`；按内容变化，不针对单一 1920×1080 硬编码。
- 移动端优先保留标题、风险、状态和下一步；metadata、图表、次级列、审计信息折叠或延后加载。
- 满足 WCAG AA 对比度；键盘焦点可见；按钮不得用 div 模拟；表单控件必须有 Label；错误与输入通过 `aria-describedby` 等方式关联；颜色不能作为唯一状态信息。
- 支持 `prefers-reduced-motion`；动画仅用于 hover、dropdown、drawer、collapse，时长 120–180ms，不使用 bounce、glow、pulse 或渐变动画。

## 8. 文案、时间与数据表达

- 动作必须具体：使用“开始调查”“核实来源 IP”“查看相关案件”“提交最终研判”“生成报告”，不要只写 Analyze、Check、Review、Next、Go。
- 列表时间：`10 分钟前`、`2 小时前`、`昨天 18:32`；截止时间：`今日 18:00`、`明日 09:00`、`已逾期 2 小时`；Audit/证据使用 `2026-08-27 17:42:18`。全项目统一。
- 同一类数字保持同一格式：小数量用 `5`、`23`，千位用 `1,024`，大数按约定使用 `10.2k`；禁止同类场景混用。
- Tooltip 只解释图标、专业术语或截断内容，不能成为理解按钮用途的唯一方式。

## 9. 内容密度预算（必须在评审时检查）

| 区域 | 首屏预算 |
| --- | --- |
| Case Header | 1 个主标题、最多 2 个 Badge、最多 5 个 metadata、1 个 Primary、最多 2 个 Secondary |
| Next Action | 最多 3 条，更多进入“查看全部” |
| Evidence | 最多 4 类摘要 |
| Related Cases | 最多 3 条 |
| Dashboard | 3 KPI + 1 趋势 + 1 关注列表 |

默认只展示会影响当前决策的信息，其余信息进入二级层。第一版只实现 Comfortable density；只有真实用户验证后才考虑 Compact，不提前加入 density switch。

## 10. 实施与变更门禁

新增页面或组件前必须：

1. 说明该页面唯一主任务和首屏决策信息。
2. 复用本文件的 tokens 与组件清单，不自行新建同义颜色/字号/状态。
3. 说明空、加载、错误、禁用、权限不足和移动端状态。
4. 若改变信息层级、导航、颜色语义、数据密度预算或组件契约，先更新本文件并记录版本历史。
5. 完成后运行项目约定的 lint、TypeScript、相关测试和 build，并进行桌面/移动端截图检查。

### Definition of Done

- [ ] 页面符合一屏一个主任务和内容密度预算。
- [ ] 颜色、字号、间距、圆角、阴影、z-index 来自 tokens。
- [ ] 风险、状态、UNKNOWN、OVERDUE 语义没有混淆。
- [ ] 八种组件状态已覆盖，关键空/错/加载态可理解。
- [ ] 键盘、对比度、Label、aria-label 和非颜色状态信息已检查。
- [ ] 无真实生产数据、客户截图或未经批准的产品边界扩展。

## 11. 版本历史

| 版本 | 日期 | 说明 |
| --- | --- | --- |
| v1.1 | 2026-08-27 | 根据已批准的总览预览和用户提供的 UI v1.0 规则建立项目级规范；补充设计上下文、内容密度预算、图标/背景素材、变更门禁，并收敛总览首屏信息量。 |
| v1.2 | 2026-08-27 | 补充用户管理页面模式：分组创建表单、不可变字段提示、行内编辑、敏感操作渐进展开及无物理删除约束。 |
| v1.3 | 2026-08-27 | 收敛用户管理的字号、行高与操作层级；身份字段默认只读，行级敏感动作统一进入“更多操作”菜单。 |
| v1.4 | 2026-08-27 | 统一业务页头与报告中心风格；明确账户设置属于个人区域，不进入一级业务导航。 |
| v1.5 | 2026-08-27 | 将账户设置入口最终确定为应用右上角用户菜单，并同步桌面端与移动端行为。 |
| v1.6 | 2026-08-27 | 增加敏感操作的可取消约束，明确取消时清空未提交凭证并关闭嵌套菜单。 |
| v1.7 | 2026-08-27 | 将用户管理调整为侧栏“管理”分组入口，区分业务导航与权限管理导航。 |
| v1.8 | 2026-08-27 | 统一业务页直接主标题起始，眉题仅在有明确分类语义时使用，避免装饰性空白。 |
| v1.9 | 2026-08-27 | 明确管理分组与业务导航的同层级视觉规则，统一字号、颜色、高度和展开状态。 |
| v2.0 | 2026-08-27 | 增加桌面顶部工作台栏规范：当前页标题、可验证的快速访问和右上角用户菜单；禁止无数据支撑的装饰性通知/搜索控件。 |
| v2.1 | 2026-08-27 | 顶部栏调整为居中快速访问与右上角用户菜单，移除重复的左侧页面名；登录页同步工作台配色并加入低对比抽象安全纹理背景。 |
| v2.2 | 2026-08-28 | 增加 Wazuh 签名 Webhook、原始告警查询与 JSONL 共用接入管线；原始 JSON 先递归脱敏再落库，Webhook 使用带时间戳的 HMAC-SHA256 并拒绝过期请求。 |
| v2.3 | 2026-08-28 | 修复接入功能导致的导航漂移：新建研判回归案件页主动作，批量导入与原始告警归入“导入”分组；恢复 SC 品牌块、统一蓝色选中态和 token 化表格容器。 |
| v2.4 | 2026-08-28 | 将侧栏“导入”分组更名为“告警接入”，子项使用“批量接入”和“原始告警”；名称覆盖 Webhook、JSONL 与后续安全数据源配置，避免把入口误解为单纯文件上传。 |

## 12. 外部告警接入安全边界

- Webhook 信任边界在 `POST /api/intake/wazuh`：仅接受配置了 `WAZUH_WEBHOOK_SECRET` 的请求，签名覆盖 `timestamp + "." + rawBody`，支持 `x-wazuh-signature`/`x-signature` 和对应 timestamp header。
- timestamp 必须在服务器当前时间前后 5 分钟内；签名使用 constant-time compare，缺少密钥、签名错误或重放请求均不进入解析和写库。
- 请求体上限 1 MB、最多 100 条；Wazuh 单对象、数组或 `{alerts: [...]}` envelope 均进入同一 `ingestAlertObject` 管线。
- 原始告警落库前递归替换 password、token、secret、authorization、cookie 等敏感键；查询 API 默认只返回接收元数据，不回显 payload。
- `externalAlertId` 仍是跨重试去重依据；重复到达保留 `RawAlertRecord`，标记 `DUPLICATE` 并关联既有案件。Webhook operationId 仅用于同一批次幂等，不替代外部 ID 去重。
- 已知限制：当前密钥由运行环境注入，尚未提供密钥轮换 UI；公网开放前必须增加 HTTPS、来源网络限制、密钥轮换和速率限制方案。
